# Pendências de modernização

Este arquivo lista o que falta atualizar no ERP com foco em **ambiente local**, sem preocupação com VPS/produção por enquanto. O objetivo aqui é tirar o sistema do estado legado: stack, dependências e frontend.

## Já modernizado

- [x] PHP 8.3 como runtime alvo, pipeline de CI atualizado para essa versão.
- [x] Dependências genéricas e bibliotecas fiscais compatibilizadas com PHP 8.3.
- [x] Laravel migrado de 8.x para 9.x, depois de 9.x para 10.x, e depois de 10.x para 11.x (ver detalhes em "Atualização da stack"). Próxima parada: 11 → 12.
- [x] `laravelcollective/html` (abandonado, sem versão compatível com Laravel 11+) substituído por um shim local próprio em `app/Support/FormHtml/` — ver detalhes em "Atualização da stack".
- [x] Laravel Mix/Webpack substituído por Vite.
- [x] Dependências principais do build frontend atualizadas, sem `npm audit` pendente na última checagem.
- [x] Corrigido carregamento de views do Superadmin e recuperado controlador do carrossel do e-commerce.

> A interface ainda usa Blade, AdminLTE e jQuery legados — o Vite modernizou o build, não a interface em si.

## Funcionalidade quebrada (bloqueia uso/testes locais)

- [x] Recuperar ou reimplementar `CteController` e `MdfeController`, junto com `app/Services/CTeService.php` e `app/Services/MdfeService.php` (não existiam). Reconstruídos do zero seguindo o padrão do módulo de NF-e (`NfeController`/`NFeService`), usando as libs já presentes `nfephp-org/sped-cte` e `nfephp-org/sped-mdfe`. CRUD completo + fluxo fiscal (gerar/transmitir/cancelar/corrigir/consultar/imprimir/zips). Não testado contra SEFAZ real (sem certificado no ambiente local) — isso fica para a etapa de homologação, fora de escopo por enquanto.
- [x] `PaymentAccountController` e `CategoryController@getCategoriesApi` também estavam referenciados em `routes/web.php` sem controlador/método correspondente. Investigado e resolvido de duas formas diferentes:
  - `CategoryController@getCategoriesApi`: implementado de verdade, seguindo o padrão de `BrandController@getBrandsApi` (API do e-commerce). O model `Category` já tinha tabela e uso real no sistema.
  - `PaymentAccountController`: era rota órfã do template original (UltimatePOS) — sem migration/tabela, sem views, sem nenhum link no sistema para `payment-account.*`. O menu "Contas de pagamento" já aponta para `AccountController`, que é a feature real e funcional. Removida a rota `Route::resource('payment-account', ...)` e o model `PaymentAccount.php` não utilizado, em vez de reconstruir uma feature fantasma.

## Atualização da stack

- [x] Migrar Laravel 8 → 9, com testes rodando no final do salto (próxima parada: 9 → 10, uma major por vez, sem pular direto para a mais nova). Detalhes:
  - Matriz de versões levantada e aplicada via `composer show <pkg> <versão> -a` para confirmar compatibilidade real (não só o número da major) antes de mexer: `laravel/framework:^9.0`, `laravel/sanctum:^3.0`, `laravel/tinker:^2.7`, `laravel/ui:^4.0`, `maatwebsite/excel:^3.1.40`, `milon/barcode:^9.0`, `spatie/laravel-activitylog:^4.12`, `spatie/laravel-backup:^8.0`, `spatie/laravel-permission:^5.5`, `srmklive/paypal:^3.0.40` (a `3.1.x` já pula para exigir Laravel 12/13), `yajra/laravel-datatables-oracle:^10.0`, `spatie/flysystem-dropbox:^3.1` (a v1 antiga travava em `league/flysystem` 1.x, incompatível com o `^3.8` que o Laravel 9 exige), `nunomaduro/collision:^6.0`.
  - **Bloqueio real encontrado**: `nwidart/laravel-menus` (usado só para o menu lateral do admin) não tem nenhuma versão compatível com Laravel 9 — a última release é de 2020, presa em `illuminate ^8.0`, e força `laravelcollective/html 6.2.*` (que também travava por causa disso, embora a versão mais recente do pacote em si já suporte L9/L10). Substituído por um builder de menu próprio em `app/Support/Menu/` (`Menu`, `MenuBuilder`, `MenuItem`, `Presenters\Presenter`), replicando só a API realmente usada no código (`Menu::create/instance/render`, `->url()`, `->dropdown()`, `->order()` — mapeado via grep, sem uso de recursos avançados do pacote original como rotas nomeadas ou dropdown multi-nível). `laravelcollective/html` promovido para dependência **direta** em `^6.4` (antes só vinha transitivo do laravel-menus) — continua no projeto porque 278 arquivos Blade usam `Form::`/`Html::` diretamente; a migração desses arquivos fica registrada abaixo, sem bloquear mais nada.
  - `fruitcake/laravel-cors` removido, trocado por `Illuminate\Http\Middleware\HandleCors` nativo do Laravel 9 no `Kernel.php`.
  - `facade/ignition` → `spatie/laravel-ignition` (`^1.7`) já feito antes do salto (dependência só de dev, troca isolada e de baixo risco).
  - **Bug real corrigido**: `BackUpController@download` usava a API do Flysystem v1 (`Storage::disk(...)->getDriver()->readStream()/getMimetype()/getSize()`), removida no Flysystem v3 que o Laravel 9 usa. Corrigido chamando `readStream()`/`mimeType()`/`size()` direto no disco (`Illuminate\Filesystem\FilesystemAdapter`).
  - **Bug real corrigido**: `spatie/laravel-activitylog` v4 tornou `getActivitylogOptions(): LogOptions` **abstrato** no trait `LogsActivity` (a API antiga baseada em propriedades estáticas `$logUnguarded`/`$logOnlyDirty` foi removida). `App\Models\DocumentAndNote` usava só as propriedades antigas e quebraria com erro fatal ao ser instanciado. Corrigido implementando `getActivitylogOptions()` com `LogOptions::defaults()->logUnguarded()->logOnlyDirty()` (comportamento equivalente ao anterior).
  - Suíte de 68 testes rodou limpa depois de cada etapa (troca do menu, `composer update` para L9, correções acima).
  - `composer audit` aponta 3 advisories em aberto no `laravel/framework` (CRLF injection na regra de e-mail, bypass de validação de arquivo) — são falhas que só têm correção em Laravel 10.48.29+/11.44.1+/12.1.1+, ou seja, inerentes a estar na 9.x; resolvem sozinhas nos próximos saltos de major, não é algo para corrigir agora.
  - Nota de ambiente (já registrada antes, reforçando): o `php` do PATH é 8.2, mas o projeto exige `^8.3` — todo `composer update`/`artisan test` precisou ser rodado com `php8.3` explícito.
- [x] Migrar Laravel 9 → 10, com testes rodando no final do salto (próxima parada: 10 → 11). Detalhes:
  - Matriz de compatibilidade levantada com `composer require <pkgs> --dry-run -W` (deixa o próprio resolvedor de dependências apontar os conflitos reais, mais direto que checar pacote por pacote como no salto anterior) antes de aplicar qualquer coisa.
  - **Bloqueios reais encontrados e resolvidos**: `eduardokum/laravel-boleto` `^0.9.3` trava em `illuminate ^8/9` → `^0.11.2` já suporta L10-12. `spatie/laravel-ignition` `^1.7` trava em `monolog ^2.3`, e o Laravel 10 exige `monolog ^3.0` → `^2.0`. `pusher/pusher-php-server` `^4.1` trava em `psr/log ^1.0`, e o `monolog ^3.0` do L10 exige `psr/log ^2.0||^3.0` → `^7.2` (dependência sem nenhum uso real no código, só a config padrão do skeleton em `config/broadcasting.php`, bump seguro). `milon/barcode` `^9.0` trava em `illuminate/support` até a 9.x → `^11.0`. Os demais pacotes diretos (sanctum, tinker, ui, maatwebsite/excel, activitylog, backup, permission, datatables-oracle, srmklive/paypal, collision, flysystem-dropbox, laravelcollective/html) já resolveram sem mudar a constraint.
  - `laravel/framework` fixado em `^10.48.29` (não só `^10.0`) para já entrar direto na primeira patch da 10.x que corrige o advisory de bypass de validação de arquivo (`PKSA-8qx3-n5y5-vvnd`/CVE-2025-27515) — a 9.x nunca recebeu esse fix por já estar fora do período de segurança.
  - **Achado sobre as advisories já documentadas no salto anterior**: das 3 abertas no L9, só essa de bypass de validação de arquivo é de fato resolvida ao chegar no L10. As outras duas (CRLF injection na regra de e-mail padrão, `CVE-2026-48019`; path confusion em signed URLs temporárias) **não têm nenhuma correção em nenhuma versão da 10.x** — os ranges afetados cobrem a 10.x inteira, com fix só a partir de Laravel 11.44.1/12.60+. Ou seja, a frase "resolvem sozinhas nos próximos saltos de major" só vale de fato a partir do salto para o L11; ficam registradas aqui de novo para não se perder de vista.
  - **Detalhe do processo de `composer update`**: a auditoria de segurança nativa do Composer (`audit.block-insecure`, ligada por padrão) bloqueia a resolução de qualquer tag 10.x por causa dessas mesmas 2 advisories sem correção na branch — os ranges delas não têm limite inferior, então "contaminam" todas as majors anteriores à correção. Sem desligar isso, o Composer cai silenciosamente para acompanhar a branch de desenvolvimento (`10.x-dev`) em vez de uma tag estável, o que seria péssimo pra reprodutibilidade (dependência sem versão fixa, podendo mudar a qualquer redeploy). Desligado via `"config": {"audit": {"block-insecure": false}}` no `composer.json` — a checagem manual das advisories continua feita à parte (ver acima), só a trava automática de resolução foi desligada.
  - **Bug real corrigido**: a propriedade `protected $dates` do Eloquent foi **removida** no Laravel 10 (vinha deprecada desde o L8/9). Todo model que ainda usava só `$dates` (sem o cast equivalente em `$casts`) passou a devolver os campos de data como string crua em vez de `Carbon`, quebrando qualquer código que chamasse métodos de `Carbon` neles — pego pela suíte de testes com um fatal em `ModuleUtil::isQuotaAvailable()` (`Call to a member function toDateTimeString() on string`) ao ler `Subscription::start_date`/`end_date`. Corrigido migrando `$dates` para `$casts` (`'campo' => 'datetime'`) em `Modules\Superadmin\Entities\Subscription`, `Modules\Superadmin\Entities\SuperadminCommunicatorLog`, `App\Models\AccountTransaction`, `App\Models\Discount` e `App\Models\CustomerGroup` — os únicos 5 models do projeto que ainda usavam a propriedade antiga.
  - Suíte de 68 testes rodou limpa depois do `composer update` e das correções acima.
  - `composer audit` aponta as mesmas 3 advisories já mencionadas acima (nenhuma nova introduzida pelo salto).
- [x] Migrar Laravel 10 → 11, com testes rodando no final do salto (próxima parada: 11 → 12). Detalhes:
  - Matriz levantada do mesmo jeito que no salto anterior (`composer require ... --dry-run -W`), resolvendo bloqueio por bloqueio até o resolvedor fechar numa combinação instalável.
  - **Bloqueio real e maior desta etapa**: `laravelcollective/html` (usado só via as facades `Form::`/`Html::`) não tem nenhuma versão compatível com Laravel 11 — última release (`v6.4.1`) trava em `illuminate/view ^10.0`, pacote abandonado desde 2023. Medido o uso real antes de decidir a estratégia: **260 arquivos Blade**, **~4.300 chamadas** `Form::` (só 16 métodos distintos usados: `label`, `text`, `select`, `checkbox`, `open`, `close`, `hidden`, `textarea`, `number`, `file`, `password`, `email`, `tel`, `submit`, `radio`, `token`; `Form::model`/`Html::*`/macros/Blade directives próprios: **zero** ocorrências). Migrar de fato para `spatie/laravel-html` (sugestão oficial do Packagist) reescreveria semanticamente todos esses pontos sem rede de testes de view para validar cada formulário — decisão consciente: em vez disso, criado um **shim local** em `app/Support/FormHtml/` (`HtmlBuilder`, `FormBuilder`, `FormHtmlServiceProvider`, `FormFacade`, `HtmlFacade`) que reimplementa só a API realmente usada, replicando o comportamento original (repopulação via `old()`, leitura de erros de validação, geração de `_token`/`_method`), registrado nas mesmas facades `Form`/`Html` em `config/app.php` — os 260 arquivos Blade não precisaram ser tocados. Cobertura de regressão para o shim em `tests/Feature/FormShimSmokeTest.php` (renderiza `products/create` e `contacts/create`, os dois maiores consumidores de `Form::`, e confere que o HTML sai íntegro). A migração de sintaxe para um builder mais moderno continua registrada como item separado e opcional abaixo — deixou de ser bloqueante para saltos de versão do Laravel.
  - Outros bloqueios reais encontrados e resolvidos, todos por versão desatualizada travando em `illuminate/* ^10`: `spatie/laravel-backup` `^8.0` → `^9.3.6` (a `9.3.6` é a mais antiga da v9 que já aceita `illuminate ^11`, evitando pular direto pra v10, que por sua vez já exige PHP 8.4/Laravel 12+); `nunomaduro/collision` `^6.0` (dev) → `^8.0` (por sua vez força `phpunit/phpunit` para `^10.5`, ver item de PHPUnit abaixo); `spatie/laravel-permission` `^5.5` → `^6.11` (mais antiga da v6 compatível com L11); `yajra/laravel-datatables-oracle` `^10.0` → `^11.1.6` (idem); `laravel/sanctum` `^3.0` → `^4.0` (exigido pelo próprio `illuminate/support ^11`).
  - **PHPUnit atualizado junto** (arrastado pela exigência do `nunomaduro/collision` v8): `^9.3.3` → `^10.5`. Rodado `vendor/bin/phpunit --migrate-configuration` para adaptar o `phpunit.xml` ao novo schema (principal mudança: bloco `<coverage><include>` virou `<source><include>`); nenhuma mudança de código de teste foi necessária, a suíte inteira (68 testes existentes + 2 novos do shim) passou de primeira.
  - Nenhuma outra breaking change de código encontrada: o projeto usa a estrutura clássica (`app/Http/Kernel.php`, `app/Console/Kernel.php`, `app/Exceptions/Handler.php`), que o Laravel 11 continua suportando normalmente para quem não migrou para o novo esqueleto `bootstrap/app.php` — não havia motivo para migrar isso agora, é cosmético/estrutural, não bloqueia nada.
  - `composer audit`: as mesmas 2 advisories abertas do salto anterior (CRLF injection na regra de e-mail padrão, `CVE-2026-48019`; path confusion em signed URLs temporárias) continuam sem correção em **nenhuma versão da 11.x** — os ranges afetados cobrem a 11.x inteira; fix real só a partir de Laravel 12.60+/12.61.1+. Mantém-se a mesma situação já documentada: resolve nos próximos saltos de major, não antes.
- [ ] Migrar Laravel 11 → versões suportadas seguintes, uma major por vez, com testes rodando entre cada salto.
- [ ] Migrar a sintaxe `Form::`/`Html::` nos 260 arquivos Blade (~4.300 chamadas) para um builder mais moderno (`spatie/laravel-html` ou Blade puro), agora que o shim local (`app/Support/FormHtml/`) já resolveu o bloqueio de versão — deixou de ser urgente, mas o shim é um paliativo, não o destino final. Refatoração gradual, sem rede de testes de view existente para cobrir cada formulário.
- [ ] Atualizar componentes acoplados ao framework que ainda não subiram além do necessário: `nwidart/laravel-modules` segue em `^4.0` — não tem `illuminate/*` fixo no `composer.json`, então não bloqueou nenhum salto até agora, mas está bem atrás da versão atual do pacote (^13); avaliar quando for pertinente.
- [ ] Substituir SwiftMailer pelo Symfony Mailer — resolvido sozinho: o `swiftmailer/swiftmailer` (abandonado) já saiu das dependências no `composer update` do salto para Laravel 9, que usa Symfony Mailer por padrão. Nada a fazer.
- [ ] Atualizar ou substituir pacotes antigos de boletos e código de barras, checando compatibilidade a cada versão do Laravel (menus, DataTables, backup, permissões e form/html já resolvidos nos saltos acima).
- [ ] Atualizar Node para uma LTS definida no ambiente de desenvolvimento; manter lockfile reproduzível.
- [ ] Inventariar e modernizar bibliotecas frontend legadas (AdminLTE, Bootstrap, jQuery e plugins).
- [ ] Definir estratégia de frontend: manter Blade modernizado ou migrar módulos novos gradualmente para Vue/React. Não reescrever tudo de uma vez.
- [ ] Atualizar MariaDB local para uma versão suportada e validar migrations, charset, collation, índices.
- [ ] Corrigir avisos de autoload e substituir dependências abandonadas do Composer/NPM.

## Qualidade e testes (para sustentar as migrações)

- [x] Infraestrutura de testes montada: banco dedicado `storeweb_test` (mysql local, separado do `storeweb` de dev para o `RefreshDatabase` do PHPUnit não apagar dados reais), `.env.testing` (gitignorado, não commitado — cada dev cria o seu) e CI (`.github/workflows/ci.yml`) com serviço MySQL 8 dedicado rodando `php artisan test`. Migrations legadas (mysql-specific, não rodam em sqlite) validadas de ponta a ponta contra esse banco. Trait `tests/Concerns/CreatesBusiness.php` criada para montar business+admin de teste reaproveitando `BusinessUtil` (mesmo caminho do registro real), em vez de duplicar a lógica de bootstrap.
- [x] Testes de login (`tests/Feature/Auth/LoginTest.php`): credenciais válidas, senha errada, usuário inexistente, negócio inativo (loga e desloga automaticamente, comportamento de `LoginController@authenticated`).
- [x] Testes de permissões/papéis (`tests/Feature/Permissions/RolePermissionTest.php`): Admin bypassa checagens via `Gate::before` (`AuthServiceProvider`), papel `Caixa#{business_id}` (padrão criado por `BusinessUtil::newBusinessDefaultResources`) acessa `/sells` mas recebe 403 em `/roles` e `/products`, papel customizado só concede as permissões sincronizadas (`syncPermissions`). Pegadinha encontrada e documentada nos comentários do teste: `User::create_user()` não hidrata defaults de coluna do banco (ex. `user_type`) no model em memória — é preciso `->fresh()` antes de `actingAs()`, senão `CheckUserLogin` derruba a request com 403 mesmo com o valor correto salvo no banco.
- [x] Testes de empresas (`tests/Feature/Business/BusinessSettingsAndLocationsTest.php`): acesso a `business/settings` e `business-location` exige `business_settings.access` (403 sem a permissão). `permitted_locations()`/`can_access_this_location()` respeitam permissões `location.{id}` por usuário — Admin (com `access_all_locations`) enxerga `'all'`, usuário restrito só vê a location liberada. Não testado o fluxo de criação de location via `POST business-location` em si: `BusinessLocationController@store`/`@create` checam `ModuleUtil::isSubscribed`/`isQuotaAvailable` antes de criar, e isso exige um registro de `Subscription` do módulo Superadmin que não existe no ambiente de teste local — ficou de fora por exigir simular todo o módulo de assinaturas só para testar o CRUD de location.
- [x] Testes automatizados para produtos, estoque, vendas, caixa, financeiro e e-commerce (login, permissões e empresas já cobertos acima). Detalhando o que falta, por área (controllers já existentes no código, sem teste ainda):
  - [x] **Produtos** (`ProductController`, `tests/Feature/Products/ProductControllerTest.php`): permissão `product.view`/`create`/`update`/`delete` por rota (403 sem a permissão), criação de produto simples via `store` (variação DUMMY criada junto), atualização de produto via `update`, exclusão via `destroy` (ajax) quando não há compra/transferência/ajuste vinculado. Ao escrever o teste de `update`, foi encontrado e corrigido um bug real: o `catch` de `ProductController@update` tinha um `print_r($e->getMessage()); die();` esquecido *antes* do `DB::rollBack()` — qualquer exceção ali matava o processo PHP inteiro sem desfazer a transação, em vez de logar e devolver a resposta de erro como o `store` já fazia. Ficou de fora: produto variável/combo (fluxo de `product_variation`/`createVariableProductVariations`), listagem `index` via DataTables (ajax) filtrada por categoria/marca/tipo, desativação (`activate`/mass actions).
  - [x] **Estoque** (`tests/Feature/Stock/StockAdjustmentAndTransferTest.php`): `StockAdjustmentController` (permissão `purchase.view`/`create`/`delete`, criação de ajuste via `store` decrementando `VariationLocationDetails`, exclusão bloqueada sem permissão), `StockTransferController` (transferência entre duas locations via `store`, quantidade sai de uma e entra na outra), `OpeningStockController` (permissão `product.opening_stock`, `save` cria a transação `opening_stock` + `purchase_line` e popula a quantidade disponível). Descoberta importante ao escrever esses testes: `TransactionUtil::mapPurchaseSell()` (chamado por ajuste/transferência de estoque) exige uma `purchase_lines` real vinculada a uma transação `purchase`/`purchase_transfer`/`opening_stock`/`production_purchase` com `status=received` para "consumir" — só criar `VariationLocationDetails.qty_available` direto não basta, a operação falha com `PurchaseSellMismatch` e a transaction inteira é revertida (silenciosamente, pois cai no mesmo redirect da rota de sucesso). Documentado no comentário do helper `createStockedProduct()`. Também foi necessário criar uma assinatura ativa de teste (`CreatesBusiness::createActiveSubscription()`, novo helper no trait) porque `StockAdjustmentController@store`/`StockTransferController@store` chamam `ModuleUtil::isSubscribed()` diretamente (não só via middleware `CheckPayment`, que é bypassado por usuário administrador) — sem assinatura, a criação real nunca acontece e só reaparece como um redirect para a mesma URL de sucesso. Ficou de fora: `ImportOpeningStockController` (importação em lote), filtro de `StockTransferController`/`StockAdjustmentController@index` por `permitted_locations()` (só a permissão de acesso à listagem foi testada), produtos variáveis/combo no ajuste/transferência.
  - [x] **Vendas** (`tests/Feature/Sales/SellPosControllerTest.php`): a venda de ponta a ponta do POS não é feita por `SellController@store` (vazio, `//` — a rota `Route::resource('sells', ...)->except(['show'])` não expõe `store`/`update`/`destroy` de verdade) e sim por `Route::resource('pos', 'SellPosController')`, então os testes miram lá. Cobrem: permissão `sell.create`/`direct_sell.access` no index e no `store`, criação de venda "direta" (`is_direct_sale=1`, sem precisar de caixa aberto) decrementando `VariationLocationDetails`, desconto percentual sobre o total (`discount_type`/`discount_amount`/`final_total` batem com o calculado por `ProductUtil::calculateInvoiceTotal`), cancelamento de venda sem permissão (403) e cancelamento com permissão restaurando a quantidade em estoque (`SellPosController@destroy` → `TransactionUtil::deleteSale`, que na prática sempre acaba apagando a transação, passando por um estado intermediário `draft` quando o status era `final`). Duas travas de payload documentadas no teste: `TransactionUtil::createOrUpdatePaymentLines()` lê todas as chaves do array de pagamento (`card_number`, `cheque_number`, etc.) sem `isset()`, então o payload de pagamento tem que replicar a struct `$dummyPaymentLine` do controller inteira; e `SellPosController@store` lê `$input['tax_rate_id']` sem `isset()` — sem essa chave no payload a venda inteira falha com "Undefined array key" antes de criar qualquer coisa. Emissão de NFC-e/NF-e vinculada à venda ficou de fora (depende de SEFAZ real, pendência de homologação fora de escopo por enquanto); edição de venda existente (`SellPosController@update`) e vendas via caixa (não-direta, com `CashRegisterController`) também ficaram de fora.
  - [x] **Caixa** (`tests/Feature/Cashier/CashRegisterControllerTest.php`): abertura de caixa via `store` (cria `CashRegister` com `status=open` + `cash_register_transactions` inicial `credit`/`initial`), tela `GET /pos/create` bloqueada (redireciona para `CashRegisterController@create`) sem caixa aberto e liberada (`assertOk`) com caixa aberto, fechamento via `postCloseRegister` (transição para `status=close` com `closing_amount`/`total_card_slips`/`closing_note`, e a tela do POS volta a exigir abertura de caixa). Também cobre a integração com `SellPosController`: venda não-direta é bloqueada sem caixa aberto (redirect para `CashRegisterController@create`, sem criar a `transaction`) e, com caixa aberto, o pagamento da venda é lançado em `cash_register_transactions` (`transaction_type=sell`) vinculado à venda via `CashRegisterUtil::addSellPayments`. Descobertas ao escrever os testes: `CashRegisterController` não tem nenhuma checagem de permissão Spatie (`can()`) — acesso é controlado só pelo middleware de sessão/assinatura; venda não-direta com `status=final` sempre monta um recibo (`receiptContent()`), que lê `$business->cidade->nome` (relação `Business::cidade()`) — sem uma `City` associada ao negócio de teste, a exceção é engolida e a venda inteira é revertida silenciosamente (mesmo padrão de erro silencioso já visto em ajuste/transferência de estoque). Ficou de fora: `getRegisterDetails`/`getCloseRegister` (views de modal), `show` (detalhes de um registro específico, sem checagem de dono), e o relatório de vendas do dia em si.
  - [x] **Financeiro** (`tests/Feature/Financial/ExpenseTest.php`, `tests/Feature/Financial/AccountControllerTest.php`): `ExpenseCategoryController` (permissão `expense.access`, CRUD via ajax) e `ExpenseController` (permissão `expense.access` + `ModuleUtil::isSubscribed` em `create`/`store`/`edit`/`update`, criação de despesa com linha de pagamento completa via `TransactionUtil::createOrUpdatePaymentLines`, exclusão via ajax). `AccountController` (permissão `account.access`, sem checagem de assinatura): criação de conta com saldo inicial (`opening_balance`), fechamento/reativação (`close`/`activate`, ambos exigem ajax), transferência entre contas (`postFundTransfer`, não é ajax-gated) e depósito externo (`postDeposit`, idem). Dois bugs reais encontrados e corrigidos ao escrever esses testes:
    - `AccountController` importava `App\Media` (classe inexistente) em vez de `App\Models\Media` — qualquer transferência de fundos (`postFundTransfer`) que chegasse ao upload de documento opcional quebrava com erro 500 `Class "App\Media" not found`. Corrigido o import.
    - `TransactionUtil::createOrUpdatePaymentLines()` chamava `insertRevenue()` (grava em `revenues`, tabela de contas a receber) para **qualquer** pagamento, em **qualquer** tipo de transação (venda, compra, despesa, ajuste de estoque...), usando `$transaction->contact_id`. Para despesas sem contato vinculado, `contact_id` é `NULL` e o insert quebra com `Column 'contact_id' cannot be null` — o `catch` de `ExpenseController@store` tem um `echo $e->getMessage(); die;` esquecido antes do tratamento de erro normal, então a requisição inteira travava. Corrigido restringindo a chamada de `insertRevenue()` a transações `type == 'sell'` (as únicas que representam receita de fato), nos dois pontos onde ela é disparada (fluxo de boleto e fluxo normal de pagamento).
    Ficou de fora: `EcommerceController`, `PedidoEcommerceController`, `ClienteEcommerceController`/`EnderecoEcommerceController`, `getProductsApi`/`getCategoriesApi` (ver item "E-commerce" abaixo), relatórios de `AccountReportsController` (`balanceSheet`/`trialBalance`/`paymentAccountReport`/link-account), `AccountController@show`/`cashFlow` (telas de extrato/fluxo de caixa, só leitura via DataTables).
  - [x] **E-commerce** (`tests/Feature/Ecommerce/StorefrontApiTest.php`, `tests/Feature/Ecommerce/EcommerceAdminTest.php`): a "API pública de produtos/categorias" mencionada aqui (`api/ecom/*` → `ProductController@getProductsApi`, `CategoryController@getCategoriesApi`, `BrandController@getBrandsApi`) depende de `Modules\Ecommerce\Entities\EcomApiSetting`, uma classe/tabela de um módulo Ecommerce pago que **não existe neste checkout** — essas 3 rotas (e o middleware `EcomApi` que as protege) dão erro fatal (Class not found) em qualquer chamada, incluindo `catch(\Exception)` não capturando o `\Error`. Não é "falta testar", é "está quebrado sem o módulo"; sem esse módulo instalado não há como testar o caminho feliz. O fluxo real de carrinho→checkout público (que a pendência original atribuía erroneamente a `PedidoEcommerceController`) está em `routes/api.php`, middleware `authEcommerce` (autentica pela `ConfigEcommerce.token`, não pelo módulo ausente): `Api\ClienteController` (cadastro/login de cliente), `Api\EnderecoController` (endereços) e `Api\CarrinhoController@salvarPedido`/`getPedido` (carrinho → `PedidoEcommerce`). Coberto no `StorefrontApiTest`. `EcommerceController` (config da loja), `ClienteEcommerceController`/`EnderecoEcommerceController` (CRUD de cliente/endereço pelo painel ERP) e `PedidoEcommerceController` (listagem/visualização de pedido, código de rastreio, `salvarVenda` — converte um `PedidoEcommerce` já pago em uma venda formal do ERP) cobertos no `EcommerceAdminTest`. Ficaram de fora: `PedidoEcommerceController@gerarNFe`/`consultarPagamentos` (dependem de SEFAZ/SDK do MercadoPago reais) e os endpoints de pagamento do `Api\CarrinhoController` (`processarPagamentoCartao/Boleto/Pix`, também SDK do MercadoPago).
    Três bugs reais encontrados e corrigidos ao escrever esses testes:
    - `ClienteEcommerceController` importava `App\Rules\ValidaDocumento` (validação de CPF/CNPJ) mas a classe nunca foi criada — `app/Rules` nem existia — então **qualquer** criação/edição de cliente e-commerce pelo painel ERP quebrava com 500. Criada `app/Rules/ValidaDocumento.php` reaproveitando o algoritmo de dígito verificador já duplicado em `PedidoEcommerceController::validaCPF()`/`validaCNPJ()`.
    - `ClienteEcommerceController::_validate()` fazia `Rule::unique('cliente_ecommerces')->ignore($request->id)`, mas `update()` nunca populava `$request->id` (nem o formulário de edição envia esse campo) — editar um cliente sem trocar e-mail/CPF sempre falhava a validação de unicidade contra si mesmo. Corrigido passando o `$id` da rota para `_validate()` e usando-o no `ignore()`.
    - (Mesma classe de problema do `AccountController`/`insertRevenue` já documentado acima, mas aqui só a título de nota: `PedidoEcommerceController@salvarVenda` também tem uma variável `$payment` não definida usada num `!empty($payment['paid_on'])` — inofensivo porque `empty()` engole variável indefinida, mas seria melhor removido numa limpeza futura.)
- [x] Testes de fluxos fiscais (`tests/Feature/Fiscal/CteGeracaoTest.php`, `MdfeGeracaoTest.php`, `NfeGeracaoTest.php`): validam a montagem/estrutura do XML de CT-e, MDF-e e NF-e (`CTeService::gerarCTe`/`MdfeService::gerarMDFe`/`NFeService::gerarNFe`) sem depender da SEFAZ real — XML bem formado (`SimpleXMLElement`), tags principais presentes (`infCte`/`infMDFe`/`infNFe`, `rem`/`dest`/`emit`, `ide`), CFOP interestadual correto, e isolamento entre empresas (403 ao acessar CT-e/MDF-e de outro negócio). Como os construtores de `CTeService`/`MdfeService`/`NFeService` exigem um certificado A1 válido mesmo só para montar o XML localmente (chamam `Certificate::readPfx` antes de qualquer coisa), foi criada a trait `tests/Concerns/CreatesFiscalCertificate.php`, que gera um certificado PKCS#12 autoassinado em runtime (`openssl_pkey_new`/`openssl_csr_sign`/`openssl_pkcs12_export`) — `NFePHP\Common\Certificate::readPfx` não valida CN nem validade, só faz o parse do PFX, então não há necessidade de simular um certificado real de homologação. Nenhum bug de produção encontrado. Também documentada uma pegadinha de teste (não é bug real): `App\Http\Middleware\SetSessionData` só popula a sessão `user` se ela ainda não existir, então trocar de admin (`actingAs`) entre duas empresas na mesma sessão de teste "gruda" no primeiro usuário — é preciso `flushSession()` entre as trocas. Ficou de fora (dependem de rede real com a SEFAZ, mesma pendência já registrada para homologação): `sign`, `transmitir`, `cancelar`, `cartaCorrecao`/`corrigir`, `consultar` dos três serviços, e a geração dos PDFs (DANFE/DACTE/DAMDFE, bibliotecas de renderização separadas da montagem do XML fiscal em si).

## Sequência recomendada

1. ~~Recuperar `CteController`/`MdfeController` para o sistema voltar a funcionar por completo localmente.~~ Feito.
2. ~~Criar cobertura de testes dos fluxos críticos.~~ Feito.
3. ~~Remover dependências abandonadas que bloqueavam o Laravel 9 (`nwidart/laravel-menus`, `fruitcake/laravel-cors`, `facade/ignition`).~~ Feito.
4. Migrar Laravel uma versão principal por vez, corrigindo código e rodando a suíte a cada etapa. **Laravel 8 → 9, 9 → 10 e 10 → 11 concluídos**; próxima parada é 11 → 12.
5. Atualizar pacotes fiscais junto de cada salto de versão.
6. Atualizar Node/MariaDB locais.
7. Modernizar a interface de forma incremental.

## Fora de escopo por agora

Só entra quando o projeto for para VPS: homologação SEFAZ real, backups/monitoramento de produção, HTTPS/domínio/WAF, LGPD comercial, CI/CD de deploy, teste de carga.