{
    "private": true,
    "scripts": {
        "dev": "vite build --mode development && vite build --mode development --config vite.dashboard.config.js && vite build --mode development --config vite.vendor.config.js && node scripts/build-vendor-js.mjs",
        "development": "npm run dev",
        "watch": "vite build --watch",
        "watch-poll": "vite build --watch --watcher.poll",
        "prod": "npm run build",
        "production": "npm run build",
        "build": "vite build && vite build --config vite.dashboard.config.js && vite build --config vite.vendor.config.js && node scripts/build-vendor-js.mjs"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^6.0.8",
        "axios": "^1.18.1",
        "lodash": "^4.18.1",
        "postcss": "^8.5.23",
        "vite": "^8.1.5",
        "vue": "^3.5.40"
    }
}
